###############################################################################
# networks.yaml — Nutanix Subnet Definitions
#
# Used by the networks module for name → ext_id lookups.
# Populate with your actual subnet names from Prism Central.
###############################################################################

subnets:
  - "VLAN-100-Production"    # Add subnet names to filter, or leave empty for all
  # - "VLAN-200-Management"
