###############################################################################
# env.yaml — Environment Configuration for cluster-01
#
# This file contains environment-specific overrides.
# The Nutanix endpoint is set here; credentials come from env vars.
###############################################################################

# Override provider settings per-cluster if different from env vars
# nutanix_endpoint: "prism-central-01.example.com"
# nutanix_port: 9440
