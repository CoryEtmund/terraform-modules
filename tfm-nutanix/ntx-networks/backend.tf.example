###############################################################################
# Example S3 Backend Configuration
#
# Copy to backend.tf and fill in your bucket details, OR let Terragrunt
# generate the backend automatically (recommended).
###############################################################################

# terraform {
#   backend "s3" {
#     bucket         = "nutanix-terraform-state"
#     key            = "nutanix/ntx-networks/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "nutanix-terraform-locks"
#   }
# }
