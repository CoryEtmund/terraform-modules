###############################################################################
# backend.tf.example — Remote State Backend
#
# Copy to backend.tf and fill in your values to enable remote state storage.
# Without this file, Terraform stores state locally in terraform.tfstate.
###############################################################################

# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket"
#     key            = "nutanix/virtual-machines/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-state-locks"
#   }
# }
